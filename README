import Head from 'next/head';
import { motion } from 'framer-motion';
import { useEffect, useState } from 'react';

const fadeInUp = {
  hidden: { opacity: 0, y: 30 },
  visible: { opacity: 1, y: 0 }
};

const fadeIn = {
  hidden: { opacity: 0 },
  visible: { opacity: 1 }
};

export default function Home() {
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const timer = setTimeout(() => setLoading(false), 1000);
    return () => clearTimeout(timer);
  }, []);

  if (loading) {
    return (
      <div className="flex items-center justify-center h-screen bg-black">
        <motion.img
          src="/logo.png"
          alt="Loading Logo"
          className="w-20 h-20 animate-pulse"
          initial={{ opacity: 0, scale: 0.5 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ duration: 1 }}
        />
      </div>
    );
  }

  return (
    <>
      <Head>
        <title>Verisync™ Group</title>
        <meta
          name="description"
          content="Strategic consultancy for ESG, predictive systems, and elite operational architecture."
        />
      </Head>

      <main className="bg-black text-white min-h-screen font-sans">
        {/* HERO */}
        <section className="flex flex-col justify-center items-center h-screen text-center bg-gradient-to-b from-black via-neutral-900 to-black">
          <motion.div className="backdrop-blur-xl p-6 rounded-xl">
            <motion.img
              src="/logo.png"
              alt="Verisync Logo"
              className="w-28 h-28 mb-4 drop-shadow-xl"
              initial={{ opacity: 0, scale: 0.8 }}
              animate={{ opacity: 1, scale: 1 }}
              transition={{ duration: 1 }}
            />
            <motion.h1
              className="text-4xl tracking-wide font-semibold"
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.3, duration: 1 }}
            >
              Verisync™ Group
            </motion.h1>
          </motion.div>
        </section>

        {/* BOOKING + PAYMENT */}
        <section className="py-24 px-6 md:px-20 bg-neutral-900">
          <motion.h2
            className="text-3xl font-bold mb-12 text-center"
            variants={fadeInUp}
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            Book a Strategy Session
          </motion.h2>

          <iframe
            src="https://calendly.com/verisync/strategy-session"
            className="w-full max-w-2xl mx-auto h-[700px] border-none rounded-xl shadow-xl"
            loading="lazy"
          ></iframe>

          <div className="text-center mt-10">
            <a
              href="https://www.paypal.com/ncp/payment/ADZMJ6R2Z8JMQ"
              target="_blank"
              rel="noopener noreferrer"
              className="inline-block bg-blue-600 text-white px-6 py-3 rounded-xl font-semibold hover:bg-blue-700 transition"
            >
              Pay with PayPal
            </a>
          </div>
        </section>

        {/* FOUNDER’S STORY */}
        <section className="py-24 px-6 md:px-20 bg-black text-white relative overflow-hidden">
          <motion.h2
            className="text-3xl font-bold mb-10 text-center"
            variants={fadeInUp}
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            transition={{ duration: 0.8 }}
          >
            Behind Verisync™
          </motion.h2>
          <motion.div
            className="max-w-4xl mx-auto text-lg space-y-6 bg-neutral-950 bg-opacity-70 p-8 rounded-xl shadow-xl backdrop-blur-md"
            variants={fadeIn}
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            transition={{ duration: 1.2 }}
          >
            <p>
              <strong>"Some people follow business trends. I design the systems behind them."</strong>
            </p>
            <p>
              The founder of Verisync™ is a forward-thinking strategist with a deep understanding of how business,
              data, and modern infrastructure align. Raised in an environment where adaptability was key, he developed
              a mindset built on foresight, resilience, and intelligent execution.
            </p>
            <p>
              His journey wasn’t traditional — but it was intentional. With a sharp eye for structure and a natural
              instinct for timing, he learned to operate ahead of the curve, turning complexity into clarity for those
              who aim to scale with purpose.
            </p>
            <p>
              Verisync™ is the result of years of study, refinement, and real-world application. Built not just to
              consult — but to engineer intelligent growth for clients who think long-term.
            </p>
          </motion.div>
        </section>

        {/* FOOTER */}
        <footer className="text-center text-gray-500 text-sm py-8 bg-neutral-950">
          © {new Date().getFullYear()} Verisync™ Group. All rights reserved.
        </footer>
      </main>
    </>
  );
}
